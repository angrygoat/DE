---
- name: Deploy porklock
  hosts: condor:&systems
  sudo: True
  gather_facts: false
  tags:
      - porklock
  roles:
      - de-porklock
  environment:
    https_proxy: "{{ proxy_env.https_proxy }}"
